<h1 style="text-align: center;">个人网站主页</h1>

这是一个可完全离线加载资源的静态个人主页。

## 本地运行

在项目目录执行：

```powershell
python -m http.server 8000
```

然后访问 `http://localhost:8000`。页面使用的脚本、中文字体、图标、头像和背景图片均保存在项目内，不需要连接外部 CDN。
