{
  "name": "zzz-xz-personal-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "npm run validate:resources && npm test && astro check && astro build",
    "preview": "astro preview",
    "check": "npm run validate:resources && npm test && astro check",
    "test": "node --test tests/*.test.mjs",
    "validate:resources": "node scripts/validate-resources.mjs"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "packageManager": "npm@11.19.0",
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "astro": "^7.2.8",
    "typescript": "^5.9.3"
  }
}
